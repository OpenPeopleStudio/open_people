# TypeScript SDK

Generated via `openapi-typescript` from `docs/api/openapi.json`.

- `npm run generate:sdk:ts` regenerates `index.d.ts`.
- Import with `import type { components, paths } from "./index";` after publishing/consuming.
